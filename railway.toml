# Railway configuration
# Use Dockerfile for cleaner Python-only build

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Environment variables can be set in Railway dashboard

