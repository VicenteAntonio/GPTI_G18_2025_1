# Configuración de Nixpacks para Railway
# Optimizado para servicios de servidor (bots y scheduler)

[phases.setup]
nixPkgs = ["python311", "bash"]

[phases.install]
# Las dependencias se instalan en el script de inicio para evitar problemas de build

[phases.build]
# Sin fase de build necesaria

[start]
cmd = "bash scripts/start_railway_services.sh"

